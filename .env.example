# Backend API origin. Baked into the bundle at BUILD time (npm run build).
# Leave empty in dev — the Vite proxy forwards /api to localhost:3000.
# For production builds set it to the deployed server, e.g.:
# VITE_API_URL=http://31.97.228.184:7003
VITE_API_URL=
